# Django Settings
DJANGO_SECRET_KEY=django-insecure-i#&f@*p6oonn%oqjd-4q84em@0s2p#6pr)phiz5@_!0&m49gmt
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SETTINGS_MODULE=budget_tracking.settings

# Database Settings
DB_NAME=budget_tracker
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Redis Settings
CACHES = {
    'default': {
        'BACKEND': 'django_redis.cache.RedisCache',
        'LOCATION': 'redis://127.0.0.1:6379/1',
        'OPTIONS': {
            'CLIENT_CLASS': 'django_redis.client.DefaultClient',
        }
    }
}

# Celery Settings
CELERY_BEAT_SCHEDULE = {
    'calculate-monthly-expenses': {
        'task': 'budget.tasks.calculate_monthly_expenses',
        'schedule': 3600.0,  # every hour
    },
    'check-budget-alerts': {
        'task': 'budget.tasks.check_budget_alerts',
        'schedule': 86400.0,  # every day
    },
} 